export interface requestDetails {
    uniID?: string,
    uniName?: {
        ar?: string,
        en?: string
    }
    departmentID?: {
        ar?: string,
        en?: string
    }
    degreeID?: string,
    departmentName?: {
        ar?: string,
        en?: string
    }
    degreeName?: {
        ar?: string,
        en?: string
    }
    studyYears?: number,
    studyLanguage?:{
        ar?: string,
        en?: string
    }
}
